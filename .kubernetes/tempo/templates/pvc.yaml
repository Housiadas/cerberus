apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "tempo.fullname" . }}-data
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "tempo.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.storage.size }}
