apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ include "vault.fullname" . }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "vault.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "vault.fullname" . }}
  http:
    - route:
        - destination:
            host: {{ include "vault.fullname" . }}
            port:
              number: {{ .Values.service.port }}
