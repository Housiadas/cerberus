meta {
  name: Create Permission
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/v1/permissions
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "read:users"
  }
}
