meta {
  name: Delete Role
  type: http
  seq: 4
}

delete {
  url: {{base_url}}/api/v1/roles/:role_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:path {
  role_id: ROLE_ID_HERE
}
