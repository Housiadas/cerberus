meta {
  name: List Roles
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/roles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:query {
  ~page: 1
  ~rows: 10
  ~orderBy: name
  ~role_id:
  ~name:
}
