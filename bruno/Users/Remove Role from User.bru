meta {
  name: Remove Role from User
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/v1/users/:user_id/role
  body: none
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

params:path {
  user_id: USER_ID_HERE
}
